{
  "//types": "vite/client covers import.meta.env in src/; node covers process.env in the integration tests.",
  "//": "The app is plain JavaScript by design. This config runs the TypeScript compiler over it in checkJs mode, so the JSDoc annotations throughout src/ are actually verified — `npm run typecheck` catches a misspelled field or a wrong argument without turning every file into .ts.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "forceConsistentCasingInFileNames": true,
    "types": ["vite/client", "node"]
  },
  "include": ["src/**/*.js", "tests/**/*.js"],
  "exclude": ["node_modules", "dist"]
}
